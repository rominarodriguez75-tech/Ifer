# Panel de agenda

Abrir `/admin/` en el servidor. La contraseña se configura mediante la variable de entorno `IFER_ADMIN_PASSWORD`; no debe escribirse en el repositorio.

El panel permite cargar especialidades, médicos y horarios. La base SQLite se crea automáticamente en `data/` y está excluida de Git porque contiene datos de pacientes.
